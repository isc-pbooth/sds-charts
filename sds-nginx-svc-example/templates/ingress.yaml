{{- if eq (toString .Values.ui.enabled) "true" }}
apiVersion: v1
kind: Service
metadata:
  namespace: {{ .Release.Name }}
  name: nginx-clusterip-svc
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm.selectorLabels" . | nindent 4 }}

---

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: {{ .Release.Name }}
  name: nginx-clusterip-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    #alb.ingress.kubernetes.io/group.name: {{ .Values.sds.ingress.albGroupName }}
    {{- if ne .Values.sds.ingress.tls.clusterIssuer "" }}
    cert-manager.io/cluster-issuer: {{ .Values.sds.ingress.tls.clusterIssuer }}
    {{- end }}
spec:
  rules:
  {{- if ne .Values.sds.ingress.hostname "" }}
  - host: {{ .Values.sds.ingress.hostname }}
    http:
  {{- else }}
  - http:
  {{- end }}
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: nginx-clusterip-svc
            port:
              number: 80
  {{- if and (eq (toString .Values.sds.ingress.tlsEnabled) "true") (ne .Values.sds.ingress.hostname "") }}
  tls:
  - hosts:
    - {{ .Values.sds.ingress.hostname }}
    secretName: {{ .Release.Name }}-tls-cert
  {{- end }}
{{- end }}
